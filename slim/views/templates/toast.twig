{# BootstrapのToastアラート #}

<div
  class="toast position-fixed top-0 end-0 m-3"
  style="opacity: 1;"
  role="alert"
  aria-live="assertive"
  aria-atomic="true"
  v-if="toast.display"
>
  <div class="toast-header">
    <div class="spinner-grow text-success spinner-grow-sm" role="status">
      <span class="visually-hidden">Loading...</span>
    </div>
    <strong class="d-inline-block me-auto px-2 text-success">AUTO RELOAD</strong>
    <span class="text-secondary" v-html="toast.latest"></span>
    <button type="button" class="btn-close" v-on:click="chengeToastDisplay(false)"></button>
  </div>
  <div class="toast-body bg-success bg-gradient text-white">最新の配信情報に自動更新しました。</div>
</div>
